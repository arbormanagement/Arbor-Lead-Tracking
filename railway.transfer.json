{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'transfer job: no build step (tsx runs the copy from source)'"
  },
  "deploy": {
    "startCommand": "npm run db:transfer",
    "restartPolicyType": "NEVER"
  }
}
