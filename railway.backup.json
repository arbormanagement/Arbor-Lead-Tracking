{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'backup service: no build step (tsx runs the dump from source)'"
  },
  "deploy": {
    "startCommand": "npm run db:backup",
    "restartPolicyType": "NEVER"
  }
}
