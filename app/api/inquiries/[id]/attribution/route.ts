// `/api/inquiries` is the canonical path since 2026-09-05; `/api/leads` is the same
// handler under its old name and stays until 2026-10-05.
export { PATCH, runtime } from "../../../leads/[id]/attribution/route";
